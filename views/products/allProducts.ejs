<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>All Products</title>
    <link rel="stylesheet" href="/styles.css">

    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f8f8f8;
            margin: 0;
            padding: 0;
        }

        h1 {
            text-align: center;
            color: #333;
            padding: 20px;
            background-color: #4CAF50;
            color: white;
            margin-bottom: 40px;
        }

        .products-container {
            display: flex;
            flex-wrap: wrap;
            justify-content: center;
            gap: 30px;
            padding: 20px;
        }

        .product-card {
            background-color: #fff;
            border: 1px solid #ddd;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            width: 300px;
            padding: 20px;
            text-align: center;
            border-radius: 8px;
            overflow: hidden;
            transition: transform 0.3s ease, box-shadow 0.3s ease;
        }

        .product-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 8px 16px rgba(0, 0, 0, 0.2);
        }

        .product-card img {
            width: 100%;
            height: auto;
            max-height: 200px;
            object-fit: cover;
            margin-bottom: 15px;
            border-radius: 5px;
        }

        .product-card h2 {
            font-size: 1.5em;
            color: #333;
            margin-bottom: 10px;
        }

        .product-card p {
            font-size: 1em;
            color: #555;
            margin: 5px 0;
        }

        .product-card strong {
            color: #333;
        }

        .product-card button {
            background-color: #4CAF50;
            color: white;
            padding: 10px 15px;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            transition: background-color 0.3s ease;
        }

        .product-card button:hover {
            background-color: #45a049;
        }
    </style>
</head>
<body>
    <h1>All Products</h1>
    
    <div class="products-container">
        <% products.forEach(product => { %>
            <div class="product-card">
                <h2><%= product.name %></h2>
                <p><strong>Category:</strong> <%= product.category %></p>
                <p><strong>Price:</strong> $<%= product.price %></p>
                <p><strong>Description:</strong> <%= product.description %></p>
                <p><strong>Stock:</strong> <%= product.stock %> items</p>
                
                <% if (product.productImages.image1) { %>
                    <img src="/<%= product.productImages.image1 %>" alt="Product Image 1">
                <% } %>
                <% if (product.productImages.image2) { %>
                    <img src="/<%= product.productImages.image2 %>" alt="Product Image 2">
                <% } %>
                <% if (product.productImages.image3) { %>
                    <img src="/<%= product.productImages.image3 %>" alt="Product Image 3">
                <% } %>
                

                <% if (product.UserId && product.UserId.name) { %>
                    <p><strong>Seller:</strong> <%= product.UserId.name %></p>  <!-- Ensure 'UserId' has 'name' -->
                <% } else { %>
                    <p><strong>Seller:</strong> Unknown</p>
                <% } %>
            </div>
        <% }); %>
    </div>
</body>
</html>
